# 项目指导
## 通用规则

- 使用中文和用户沟通。
- 仅需要写CSharp脚本和Shader，代码可以自动写作，并都写在.\Assets\Scripts。可以用命名空间，可以由子文件夹。必要时才调整其它美术、音乐等资源的属性。如需修改项目设置，请跟用户沟通，由用户修改。
- 游戏需要兼容2D PC端和移动端，其中移动端最重要。
- 本机仅有代码编写功能，无Unity，调试由用户在另一台PC端手动调试。
- 不进行git操作，但是给予git commit message的指导
- 变量名用英语，并简单易懂。
- 每个类和方法添加标准中文注释，简单逻辑不需要添加注释，复杂逻辑添加中文注释。
- 不要因为格式化的修改而大改仓库。减少修改量。
- 禁止擅自修改README.md和Agent.md。在用户明确要求下，才能写文档。
- 高内聚，低耦合。

## 游戏总规则

以下是游戏的总设计，做参考。

### 游戏背景
这是《边狱巴士》（简称“边巴”）蜘蛛巢章节相关人格的Unity 2D同人游戏。由于边巴的一个游戏机制是角色“良秀”砍队友可以获得强力buff，我们模仿“切水果”游戏，做到砍队友（以下称为“character”）的地狱效果。

### 技术要求
仅作总体大纲。每次脚本写作的具体要求请看用户的输入。
仅包含基础功能。

#### 边界获取脚本
要有一个专门的脚本，提供方法获取屏幕边界与长宽。

#### 开始界面
点击开始进入游戏。

#### Character生成
需要一个控制器脚本，存储一个预制体（_alive后缀的character）列表。按波次生成1~多个实体。关卡的一开始，一波次只会随机生成1~2个character，接下来1~3个，后面越来越多，生成速度越来越快。

#### Character未被切时的运动
不使用Unity自带的物理演算。
character从屏幕的左/右/下边界随机生成，以不会超过上边界的抛物线进行运动。运动轨迹不宜过短。同时，应该也有初始的随机力矩，使其做旋转运动，不宜转动过快。

#### 刀光
手指/鼠标（以下都称手指）拂过的时候有白色轨迹，这样的轨迹有四个角（流线），起始点和终点都是一个角，并且起始点对应角是个钝角，终点大概率是个锐角。靠近终点不透明度有所降低。

#### Character被切判定
手指划过，可以使用Character上的碰撞体判定入点和离点。请注意，Character绑定了2D多边形碰撞体，是is_trigger。请选择合适的判定方式。判定被切后，要得到Character刀痕入点与出点的相对坐标，以便于下一步切开效果的生成。

#### Character被切后变化
被切后，在同一位置和角度生成两个_dead Character实体，同时删除_alive Character。需要一个shader，按照被切的直线，两个_dead Character实体分别呈现被切的两半

#### Character被切后运动
两个_dead Character在保留原有运动状态的同时，以一个合适的反馈分别添加两个飞出的力，达到让玩家解压的效果。

#### 加分条件
暂定为切一个Character加1分，后续再加料。

#### 失败条件
有3条命。当Character超出屏幕范围三次时，游戏结束。

#### 结算界面
显示分数。若破纪录有特殊显示。
再玩一次按钮。

### 美术资源
Assets\Arts\Prefabs：预制体。目前只有YiSang_alive和YiSang_dead作为测试预制体。YiSang_alive预制体内含2D多边形碰撞体，是is_trigger。
Assets\Arts\Textures：纹理。
Assets\Arts\UI：UI，由用户制作。尚未制作。

### 代码资源
Assets\Scripts：代码文件夹，由你来写作。
已经新建的脚本：
Assets\Scripts\BeCutted.cs：character被砍的判定。
Assets\Scripts\GameController.cs：游戏的总控制器，预计绑定在场景内的GameController上。
